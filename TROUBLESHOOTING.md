# Troubleshooting 

## 최근 커밋 메세지 수정하기

가장 최근 커밋 메시지만 수정하려면 아래 명령어를 사용한다.
```bash
git commit --amend -m "수정할 커밋 메시지"
```

수정 결과 확인 방법
```bash
git log --oneline -1
```

단, `git push`까지 한 커밋의 메시지를 수정하면, 내 로컬 커밋 기록과 GitHub 원격 커밋 기록이 서로 달라진다.
`git commit --amend`는 기존 커밋을 수정하는 것처럼 보이지만 사실 새 커밋을 만드는 것이기 때문이다.

로컬의 수정된 커밋 기록으로 원격 브랜치를 갱신하려면 다음 명령어를 사용한다.
```bash
git push --force-with-lease
```
내 로컬 커밋 기록을 기준으로 원격 브랜치를 갱신하되, 그 사이에 다른 사람이 원격에 새 커밋을 올렸다면 덮어쓰지 말고 중단하라는 의미이다.

`--force-with-lease`는 원격 브랜치에 내가 모르는 새 커밋이 생겼는지 확인한 뒤 push한다.
따라서 단순 `--force`보다 안전하다.

주의:
- 혼자 작업하는 과제 브랜치에서는 보통 사용해도 괜찮다.
- 협업 브랜치에서는 다른 사람의 커밋 기록을 덮어쓸 수 있으므로 사용 전에 반드시 주의해야 한다.