### 객체지향적 설계
- 필요한 데이터를 먼저 나열하고 객체를 생성하는 것보다, 이 프로그램이 해야 하는 일을 보고 그 일을 수행할 책임의 주체를 찾는 식으로 접근
- 기능을 묶는 것이 아니라, 그 기능을 가장 잘 알고 있고 책임질 수 있는 객체에게 맡기기

### 필요 기능
#### 기능 선택 기능
#### 1. 기능 출력
- 프로그램이 시작되면 기능 목록이 출력된다.
- 사용자가 선택한 기능이 끝나면 기능 목록이 출력된다.

#### 2. 사용자 입력
- 사용자가 원하는 기능 번호를 입력한다.

#### 3. 입력값 검증
- 기능 번호가 숫자가 아니거나 유효한 번호가 아니면 예외가 발생한다.

#### 4. 기능 연결
- 사용자가 입력한 값이 1이면 상영 목록 조회 기능을 제공한다.
- 사용자가 입력한 값이 2라면 예매 기능을 제공한다.
- 사용자가 입력한 값이 3이라면 예매 조회 기능을 제공한다.
- 사용자가 입력한 값이 4라면 예매 취소 기능을 제공한다.
- 사용자가 입려간 값이 5라면 프로그램을 종료한다.

---

#### 영화 예매 기능
##### 1. 사용자 입력
- 사용자가 휴대폰 번호를 입력한다.
- 사용자가 상영 번호를 입력한다.
- 사용자가 예매할 좌석 목록을 입력한다.

##### 2. 입력값 검증
- 휴대폰 번호가 비어 있으면 예외가 발생한다.
- 상영 번호가 숫자가 아니면 예외가 발생한다.
- 좌석 목록이 비어 있으면 예외가 발생한다.
- 좌석 목록이 1개 미만이면 예외가 발생한다.

##### 3. 상영 번호 확인
- 입력한 상영 번호에 대한 상영이 존재하는지 확인한다.
- 유효하지 않은 상영 번호이면 예외가 발생한다.

##### 4. 좌석 목록 확인
- 선택한 좌석이 해당 상영에 존재하는지 확인한다.
- 존재하지 않는 좌석이면 예외가 발생한다.
- 선택한 좌석이 이미 예매된 상태인지 확인한다.
- 이미 예매된 좌석이면 에외가 발생한다.

##### 5. 예매 처리
- 예매 번호를 생성한다.
- 선택한 좌석들을 예매된 상태로 변경한다.
- 예매 정보를 생성한다.
- 예매 정보를 저장한다.

##### 6. 결과 출력
- 예매 성공 메시지를 출력한다.
- 생성된 예매 번호를 함께 출력한다.

---
#### 예매 조회 기능
#### 1. 사용자 입력
- 사용자가 휴대폰 번호를 입력한다.

#### 2. 입력값 검증
- 휴대폰 번호가 비어있으면 예외가 발생한다.

#### 3. 예매 기록 조회
- 휴대폰 번호로 예매 기록을 조회한다.
- 휴대폰 번호가 예매 기록에 존재하지 않으면 예외가 발생한다. 

#### 4. 결과 출력
- 조회된 예매 기록을 출력한다.

